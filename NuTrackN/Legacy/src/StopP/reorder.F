	PROGRAM REORDER
	CHARACTER*20 INPFIL,OUTFIL
	CHARACTER*80 BLABLA
C	TO WRITE OUTPUT FILE FOR HPPLOT
C	INPUT FILE IS THE OUTPUT FILE FROM STOPP
	WRITE(5,'('' Enter the input filename  ; '',$)')
	READ(5,'(A)')INPFIL
	WRITE(5,'('' Enter the output filename ; '',$)')
	READ(5,'(A)')OUTFIL
	OPEN(UNIT=1,NAME=INPFIL,STATUS='OLD',READONLY)
	OPEN(UNIT=2,NAME=OUTFIL,STATUS='NEW')
	DO II=1,14
	 READ(1,'(A)')BLABLA
	ENDDO
	DO II=1,10000
	  READ(1,*,END=100)A,B,C,D,E,F,G
	  WRITE(2,'(F12.4,4X,F12.4)')G,E
	ENDDO	
100	CLOSE(UNIT=1)
	CLOSE(UNIT=2)
	CALL EXIT
	END
